export const htmlContent = `<p><strong>API Permissions</strong></p><p>Security Justification &amp; Data Governance</p><p>This document outlines the purpose, necessity, and security posture of each API permission configured for the application. It is intended to provide full transparency regarding how these permissions are used, what safeguards are in place, and why each permission is the minimum required to deliver the service.</p><h1>1. Our Core Security Principles</h1><table><tr><td><p><strong>Guiding Philosophy: Zero Trust &amp; Least Privilege</strong></p><p>Every permission configured in this application follows the principle of least privilege — we request only what is strictly necessary, only when it is needed, and only on behalf of the authenticated user.</p></td></tr></table><p>The following principles govern all data access within our platform:</p><ul><li>User Impersonation Only: All permissions are of Delegated type, meaning the application acts on behalf of the signed-in user — never autonomously or with its own elevated identity.</li><li>No Background Operations: The system performs no scheduled jobs, background syncs, or silent data collection. Every operation is triggered explicitly by a user action.</li><li>Bounded by User’s Own Permissions: If a user does not have access to a resource in the Azure environment, our app cannot access it either. We cannot exceed the user’s native permissions.</li><li>No Data Retention of Sensitive Assets: We do not store, cache, or replicate raw data from your Azure environment. Metadata retrieved for display or query purposes is transient.</li><li>Consent-Driven: All access requires the user’s active session and consent. No permission allows us to operate without a user’s explicit request.</li></ul><h1>2. Permission-by-Permission Justification</h1><h2>2.1  Azure Storage — user_impersonation</h2><table><thead><tr><th><p><strong>Permission Scope</strong></p></th><th><p><strong>Type</strong></p></th><th><p><strong>Purpose &amp; Justification</strong></p></th><th><p><strong>What We Do NOT Do</strong></p></th></tr></thead><tbody><tr><td><p>user_impersonation</p></td><td><p>Delegated</p></td><td><p>Used to read output artifacts stored in Azure Blob Storage — specifically the response payloads generated by Python code executed within the client’s own environment in response to a user query. This is a read operation on designated output containers only.</p></td><td><p>No write, delete, or overwrite of storage data. No access to arbitrary storage accounts or containers beyond designated output paths.</p></td></tr></tbody></table><table><tr><td><p><strong>Why Azure Storage access is needed</strong></p><p>When a user submits a natural language query that triggers Python execution in their environment, the result is written to a pre-configured Azure Blob Storage location within the client’s own tenant. Our application reads from that location to retrieve and display the response. This is a targeted, scoped read — not general storage browsing.</p></td></tr></table><h2>2.2  Microsoft Graph — User.Read</h2><table><thead><tr><th><p><strong>Permission Scope</strong></p></th><th><p><strong>Type</strong></p></th><th><p><strong>Purpose &amp; Justification</strong></p></th><th><p><strong>What We Do NOT Do</strong></p></th></tr></thead><tbody><tr><td><p>User.Read</p></td><td><p>Delegated</p></td><td><p>Reads the authenticated user’s basic profile (name, email, user ID, tenant ID). Used for identity resolution, display personalization, and associating user actions with the correct account during the session.</p></td><td><p>No access to other users’ profiles, group memberships, mailboxes, calendars, or directory data.</p></td></tr></tbody></table><table><tr><td><p><strong>Minimum viable identity scope</strong></p><p>User.Read is the most restricted identity scope available in Microsoft Graph. It grants access only to the signed-in user’s own profile — nothing else. We do not request User.ReadBasic.All, User.ReadAll, or any directory-level permissions.</p></td></tr></table><h2>2.3  Power BI Service — Delegated Permissions</h2><p>The Power BI Service permissions are the backbone of our platform’s core functionality. All permissions are Delegated, meaning they operate strictly within the signed-in user’s own access rights — the application can never access more than the user themselves is permitted to see.</p><p>These permissions collectively enable four key functional areas of the platform:</p><ul><li>Lakehouse &amp; Semantic Model Discovery: We read workspace contents, dataset definitions, and Lakehouse connection metadata so users can explore and query their data assets. This includes understanding which datasets are backed by Azure Data Lake or Synapse, and surfacing the right schema information for user queries.</li><li>Workspace &amp; RBAC Awareness: We read workspace membership and role assignments to accurately reflect what the user has access to, and to respect permission boundaries within their organisation. We never modify workspace membership or user roles.</li><li>On-Demand Query Execution: When a user submits a query, we execute it against the relevant semantic model or dataset on their behalf using their own credentials. All executions are user-triggered — there are no scheduled or background operations.</li><li>Capacity &amp; App Metadata: We read capacity and app information to understand the environment’s configuration and present the correct options to the user. No capacity changes or app modifications are made without explicit user instruction.</li></ul><table><tr><td><p><strong>Why are there so many permission scopes?</strong></p><p>The Power BI REST API is highly granular — Microsoft divides functionality into many fine-grained scopes (e.g. separate scopes for reading reports vs. dashboards vs. datasets vs. workspaces). The number of scopes reflects the breadth of Power BI features our platform supports, not the breadth of data we access. Each scope is the minimum required for a specific feature, and all are Delegated — always bounded by what the signed-in user can access.</p></td></tr></table><h1>3. What We Explicitly Do Not Request</h1><p>As important as what we request is what we deliberately choose not to request. The following categories of permissions are not configured and are not used:</p><table><tr><td><p><strong>Permission Category</strong></p></td><td><p><strong>Why We Do Not Request This</strong></p></td></tr><tr><td><p><strong>Application Permissions (non-delegated)</strong></p></td><td><p>All permissions are Delegated. Application permissions would allow server-to-server access independent of user sessions — we do not use these anywhere in the platform.</p></td></tr><tr><td><p><strong>User directory enumeration (User.ReadBasic.All, Directory.Read.All)</strong></p></td><td><p>We only need the signed-in user’s identity. We have no need to enumerate other users, groups, or directory objects.</p></td></tr><tr><td><p><strong>Mail, Calendar, Teams, SharePoint</strong></p></td><td><p>The platform has no communication or collaboration features. These APIs are entirely outside our scope.</p></td></tr><tr><td><p><strong>Key Vault direct secret access</strong></p></td><td><p>We do not read, modify, or manage secrets, keys, or certificates in Key Vault directly.</p></td></tr><tr><td><p><strong>Storage write or delete</strong></p></td><td><p>All Storage access is read-only for designated output containers.</p></td></tr><tr><td><p><strong>SQL DML / DDL execution</strong></p></td><td><p>We do not issue data manipulation or schema modification statements against any SQL endpoint.</p></td></tr><tr><td><p><strong>Power BI admin APIs</strong></p></td><td><p>We use standard user-context APIs only. Admin-level APIs that bypass workspace RBAC are not requested.</p></td></tr></table><h1>4. How User Impersonation Protects the Client</h1><p>All permissions in this configuration are of type Delegated with user_impersonation or user-context scopes. This is a critical design choice with significant security implications:</p><table><tr><td><p><strong>What ‘Delegated’ means in practice</strong></p><p>A Delegated permission means the application presents the user’s own identity token to the downstream Azure service. The Azure service evaluates the request as if the user themselves made it — applying all of the same RBAC, row-level security, and access controls that would apply to that user directly.</p></td></tr></table><ul><li>If the user does not have access to a specific workspace, dataset, or storage container, our application cannot access it either.</li><li>The application cannot access resources when the user is not logged in. There is no persistent background token or service principal operating independently.</li><li>Every access event is attributable to an authenticated user identity and will appear in your Azure audit logs accordingly.</li><li>Revoking a user’s Azure AD access immediately revokes the application’s ability to act on their behalf.</li></ul><h1>5. Audit, Transparency &amp; Client Controls</h1><p>Clients retain full visibility and control over all access made through these permissions:</p><ul><li>Azure AD Sign-in Logs: All delegated access events are recorded in your Azure Active Directory sign-in logs, attributable to the user’s identity.</li><li>Power BI Activity Log: All Power BI API calls (dataset queries, workspace reads, etc.) are logged in the Power BI activity log available to tenant admins.</li><li>Conditional Access Policies: Your organization’s existing Conditional Access policies apply to our application. MFA requirements, device compliance, and IP restrictions are all honoured.</li><li>Revocation: Admin consent can be revoked at any time from the Azure AD Enterprise Applications portal, immediately terminating all access.</li><li>Scope Review: Clients may request a full review of API call logs or a detailed technical walkthrough of how each permission is exercised at any time.</li></ul><h1>6. Summary</h1><table><tr><td><p><strong>Our Commitment</strong></p><p>We have configured the minimum permissions necessary to deliver the platform’s features. Every permission is Delegated (user-context only), every operation is user-initiated, and your organization’s own Azure RBAC controls remain the ultimate gatekeeper for all access. We do not collect, store, or transmit raw data from your environment beyond what is required to render a response to an active user request.</p></td></tr></table><p>We welcome any further questions, a live technical walkthrough, or a formal security review engagement. Our goal is to ensure you have complete confidence in the security posture of this integration.</p><p><em>Prepared for client review. Please contact your account team for further clarification on any permission listed in this document.</em></p>`;
