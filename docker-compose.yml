services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: karl-homepage:latest
    container_name: karl-homepage-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
